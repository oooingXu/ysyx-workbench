menu "ISA-dependent Options for riscv"

config RV64
  bool "64-bit RISC-V architecture"
  default n

config RVE
  bool "Use E extension"
  default n

config RVA
  bool "Use A extension"
  default n

config RVC
  bool "Use C extension"
  default n

config RVDF
  bool "Use DF extension"
  default n
endmenu

menu "CSR-dependent Options for riscv"

config MEPC
	bool "Use mepc csr"
	default y

config MCAUSE
	bool "Use mcause csr"
	default y

config MTVEC
	bool "Use mtvec csr"
	default y

config MSTATUS
	bool "Use mstatus csr"
	default y

config MIE
	bool "Use mie csr"
	default y

config MSCRATCH
	bool "Use mscratch csr"
	default y

config MTVAL
	bool "Use mtval csr"
	default y

config MIP
	bool "Use mip csr"
	default y

config MIMPID
	bool "Use mimpid csr"
	default n

config MHARTID
	bool "Use mhartid csr"
	default n

config MVENDORID
	bool "Use mvendorid csr"
	default n

config MARCHID
	bool "Use marchid csr"
	default n

endmenu

